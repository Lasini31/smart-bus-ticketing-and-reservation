server:
  port: 8081

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://rpbufjokybeukqcdfumq.supabase.co/auth/v1/.well-known/jwks.json
          jws-algorithm: ES256

app:
  auth:
    mock: false
